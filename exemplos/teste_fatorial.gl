int fatorial(int n) {
    // Caso base
    if (n < 2) {
        return 1;
    }
    // Chamada recursiva: n * fatorial(n-1)
    return n * fatorial(n - 1);
}

int main() {
    // Calcula 5! (5 * 4 * 3 * 2 * 1)
    return fatorial(5); 
}